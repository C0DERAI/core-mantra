---
name: mantra:stupefy
description: Toggle ultra-compressed communication mode. Drops filler and pleasantries, keeps full technical accuracy. ~75% token reduction.
---

When invoked:

1. Invoke `core/mode-detector`. If already in `lite` mode, stupefy is already active — confirm tersely and return.
2. Invoke the `discipline/stupefy` skill and follow it exactly.
3. Do not produce a summary, explanation, or acknowledgement beyond a single terse confirmation.

To deactivate: user says "stop stupefy" or "normal mode".
